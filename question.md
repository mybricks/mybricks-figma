- style属性
- margin的处理
  - 间距一致用自动布局 + gap，否则用 x / y (还原回来diff怎么还原)
- 全局字体，两边不同步
- 同步过去，只能生成不能同步，不然还要绑定
- figma支持变体 + 组件 + 实例，可以做到生成修改组件，实例跟着修改，实例也可以上锁，多个变体dom可以不一样
- 锁 ≈ 禁止在画布上选中和拖拽，不禁止通过图层面板选中后改配置项。


- 同步过去的问题
  - 各类结构转化（会导致同步回来的时候）
    - margin -> gap / x/y
    - 文本节点没有内间距的概念，div + 文本实现标签的情况，转过去之后会需要两个不同的dom来承载样式
    - 文字是继承的是div属性，同步回来的话需要绑定会原来的class
    - 文字阴影
  - 变体：除了代码组织外，还需要一个地方真实渲染，不然拿不到渲染后样式
  - 列表使用组件和实例：组件修改通用属性，实例跟着修改，看了下figma是支持的
  